{{- if .Values.serviceMonitor.enabled }}
{{- $fullname := include "pythonapps.fullname" . -}}
{{- $svcName := ternary (printf "%s-stable" $fullname) $fullname (or (eq .Values.rollout.strategy "bluegreen") (eq .Values.rollout.strategy "canary")) -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ $fullname }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pythonapps.labels" . | nindent 4 }}
    release: kube-prometheus
spec:
  selector:
    matchLabels:
      {{- include "pythonapps.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: http
    path: {{ .Values.app.metricsPath }}
    interval: {{ .Values.serviceMonitor.interval }}
{{- end }}
